[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-native-spec-pipeline"
version = "0.1.0"
description = "Prototype AI-native, spec-driven development pipeline with deterministic governance controls."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "jinja2>=3.1",
  "pydantic>=2.6",
  "PyYAML>=6.0",
  "uvicorn>=0.30",
]

[project.optional-dependencies]
dev = [
  "bandit>=1.7",
  "httpx>=0.27",
  "mypy>=1.10",
  "pytest>=8.0",
  "ruff>=0.6",
]

[tool.setuptools.packages.find]
include = ["pipeline*", "pipeline_web*"]
where = ["."]

[tool.setuptools.package-data]
pipeline_web = ["templates/*.html", "static/*.css", "static/*.js"]

[tool.pytest.ini_options]
pythonpath = [".", "demo_app/src"]
testpaths = ["tests", "demo_app/tests"]
markers = [
  "acceptance: tests mapped to feature acceptance criteria",
  "integration: cross-component workflow tests",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "S"]
ignore = ["S101", "S310", "S603", "S607"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
strict = false
warn_unused_ignores = true
warn_return_any = true
warn_unreachable = true
